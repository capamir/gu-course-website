import { Box } from "@chakra-ui/react";
import { CourseHeading } from "../components";

const Home = () => {
  return (
    <Box>
      <Box paddingRight={2}>
        <CourseHeading />
      </Box>
    </Box>
  );
};

export default Home;
